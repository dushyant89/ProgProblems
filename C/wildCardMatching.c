#include<stdio.h>
#include<string.h>
int main()
{
   char pattern[100];
   char input[100];
   while(1) {

       printf("Enter the pattern\n");
       scanf("%s",pattern);

       printf("Enter the input\n");
       scanf("%s",input);

       int len_patt=strlen(pattern);
       int len=strlen(input);

   }

   return 0;
}
